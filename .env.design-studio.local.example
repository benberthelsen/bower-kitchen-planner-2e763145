# Copy this file to .env.design-studio.local to test the real AI locally.
# The *.local file is gitignored and these values are never bundled into the browser.
OPENAI_API_KEY=replace-with-your-private-server-key
OPENAI_MODEL=gpt-5.6-terra
